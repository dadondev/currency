"use client";

import styled from "styled-components";

const Container = styled.article`
  width: auto;
  margin: 0 auto;
  text-align: center;
`;

const OverLessons = () => {
  return (
    <Container>
      <span>Darsi yakunlangan sinflar</span>
    </Container>
  );
};

export default OverLessons;
